---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="SISSPA — Office 365">
  <Header />

  <section class="sti-hero section">
    <div class="container">
      <div class="sti-top">
        <div class="section-label">Promoción</div>
        <h1>Avanza al siguiente <span class="text-gradient">nivel</span> en tu negocio</h1>
        <p class="sti-sub">Contando con el apoyo tecnológico de SISSPA</p>
      </div>

      <div class="sti-grid">
        <div class="sti-info">
          <div class="sti-block">
            <h3>Con nuestro servicio de soporte TI podrás:</h3>
            <ul>
              <li><i class="fas fa-check"></i> Contar con un equipo técnico dispuesto a atender las necesidades de tu negocio.</li>
              <li><i class="fas fa-check"></i> Respaldo de información de tus equipos para continuidad operacional.</li>
              <li><i class="fas fa-check"></i> Asesoría profesional para tus proyectos tecnológicos.</li>
              <li><i class="fas fa-check"></i> Instalación y actualización de softwares.</li>
            </ul>
          </div>

          <div class="sti-block">
            <h3>Recibe sin costo adicional 1 año de Office 365 para 6 usuarios:</h3>
            <ul>
              <li><i class="fas fa-check"></i> Microsoft Word, Excel, PowerPoint y Outlook</li>
              <li><i class="fas fa-check"></i> 6TB de almacenamiento en OneDrive (1TB por usuario)</li>
              <li><i class="fas fa-check"></i> Seguridad avanzada de OneDrive</li>
              <li><i class="fas fa-check"></i> Instalación y puesta en marcha gratis</li>
            </ul>
          </div>

          <div class="sti-badge">
            <img src="/images/sisspa/office-365.png" alt="Office 365" />
          </div>
        </div>

        <div class="sti-form-card">
          <h3>Solicita tu servicio</h3>
          <p>Completa el formulario y nos pondremos en contacto contigo.</p>
          <form action="#" name="365" method="POST" data-netlify="true">
            <input type="text" name="name" placeholder="Nombre" required />
            <input type="email" name="email" placeholder="Email" required />
            <input type="tel" name="tel" placeholder="Teléfono" />
            <input type="text" name="subject" placeholder="Empresa" />
            <textarea name="body" placeholder="Deja tu mensaje" required></textarea>
            <button type="submit" class="btn btn-primary">Solicitar Servicio</button>
          </form>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .sti-hero {
    padding-top: 140px;
  }

  .sti-top {
    text-align: center;
    margin-bottom: 64px;
  }

  .sti-top h1 {
    margin-bottom: 12px;
  }

  .sti-sub {
    font-size: 1.125rem;
    color: var(--text-3);
  }

  .sti-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: start;
  }

  @media (max-width: 768px) {
    .sti-grid { grid-template-columns: 1fr; gap: 40px; }
  }

  .sti-block {
    margin-bottom: 32px;
  }

  .sti-block h3 {
    margin-bottom: 16px;
  }

  .sti-block ul {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .sti-block li {
    font-size: 0.9375rem;
    color: var(--text-2);
    display: flex;
    align-items: baseline;
    gap: 10px;
    line-height: 1.6;
  }

  .sti-block li i {
    color: var(--accent);
    font-size: 0.7rem;
    flex-shrink: 0;
  }

  .sti-badge {
    background: var(--bg-2);
    border-radius: var(--r);
    padding: 20px 28px;
    display: inline-block;
  }

  .sti-badge img {
    height: 36px;
  }

  .sti-form-card {
    background: var(--bg-2);
    border: 1px solid var(--border);
    border-radius: var(--r);
    padding: 40px;
  }

  .sti-form-card h3 {
    margin-bottom: 8px;
  }

  .sti-form-card > p {
    font-size: 0.9375rem;
    color: var(--text-3);
    margin-bottom: 24px;
  }

  .sti-form-card form {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .sti-form-card input,
  .sti-form-card textarea {
    width: 100%;
    padding: 10px 14px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 8px;
    color: var(--text);
    font-size: 0.875rem;
    font-family: var(--font);
    transition: border-color 0.2s;
    outline: none;
  }

  .sti-form-card input::placeholder,
  .sti-form-card textarea::placeholder {
    color: var(--text-3);
  }

  .sti-form-card input:focus,
  .sti-form-card textarea:focus {
    border-color: var(--accent);
  }

  .sti-form-card textarea {
    min-height: 100px;
    resize: vertical;
  }

  .sti-form-card .btn {
    width: 100%;
    justify-content: center;
    margin-top: 6px;
  }
</style>
